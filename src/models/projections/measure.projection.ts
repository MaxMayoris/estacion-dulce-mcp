/**
 * Measure projections for efficient data transfer
 */

/**
 * Measure index projection - minimal data for listing
 */
export interface MeasureIndexProjection {
  id: string;
  name: string;
  unit: string;
}
