/**
 * Category projections for efficient data transfer
 */

/**
 * Category index projection - minimal data for listing
 */
export interface CategoryIndexProjection {
  id: string;
  name: string;
}
